# ---------------------------------------------
#              TP 3 -  Exercices
# ---------------------------------------------

## Exercices  -----

# NOM
# PRENOM

# **Exercice 1.1. ** -----
# Soit deux tableau d1 et d2, assembler les deux tableaux l'un en dessous de l'autre et afficher le résultat
#
# library(tidyverse)
# d1 <-tibble(
#   a= c(1,2,3,4),
#   b=c(5,6,7,8)
# )
# d1
#
# d2 <-tibble(
#   a= c(5,6,7,8),
#   b=c(9,10,11,12),
#   c=c(1,2,3,4)
# )
# d2


# **Exercice 1.2. ** -----
# Soit deux tableau d3 et d4, assembler les deux tableaux l'un à côté de l'autre

# d3 <-tibble(
#   a= c(1,2,3,4),
#   b=c(5,6,7,8)
# )
# d3
#
# d4 <-tibble(
#   c= c(5,6,7,8),
#   d=c(9,10,11,12)
# )
# d4



# **Exercice 2.  ** -----

# A partir des données d'expulsion faite la somme des expulsions par commune et afficher les 3 communes avec le plus d'expulsions (Données disponibles: "expulsions_2018_secteursstat.csv" et "OPEN DATA_SECTOREN_2019.xlsx" ) [enchainer les opérations avec des %>%  et commenter chaque lignes avec des #]




# **Exercice 3.  ** -----

# Sur le site du monitoring des quartiers de l'IBSA il est possible (pour les utilisateurs avec un compte) de télécharger des données par secteurs statistiques. Malheureusement, certains code de secteurs statistiques subissent une modification par Excel. A partir d'un fichier issu de ce site (mq_data_export_2024-02-26.xlsx) et du fichier de statbel (OPEN DATA_SECTOREN_2019.xlsx) identifier les secteurs statistiques problématiques (faites le une fois avec une jointure et avec un notin)



